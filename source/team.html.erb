<div class="container text-center">
  <h1>Notre équipe</h1>
  <%members=%w(anne seb romain)%>
    <ul class="list-inline text-center">
      <% members.each do |member| %>
        <li>
          <%= image_tag "#{member}.jpg", class: "img-circle" %>
          <h3><%= member.capitalize %></h3>
        </li>
      <% end %>
    </ul>
</div>
